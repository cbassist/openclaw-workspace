# Symlink to live npm install (don't version the target)
install

# Editor/OS artifacts
.DS_Store
.idea/

# Secrets
.env
dante/.env
**/secrets.json

# Telethon session files (persistent auth — NEVER commit)
*.session
*.session-journal

# Dependencies and build artifacts
node_modules/
dist/
__pycache__/
*.pyc
.venv/

# Workspace tooling (Playwright, etc.)
package.json
package-lock.json

# Test artifacts
test-results/
